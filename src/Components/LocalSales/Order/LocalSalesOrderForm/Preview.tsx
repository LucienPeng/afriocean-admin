import { Stack } from '@mui/material';

export const Preview = () => {
  return (
    <Stack
      sx={{
        width: '100%',
        minHeight: '30vh',
        border: '1px solid',
        borderColor: 'primary.main',
        borderRadius: '8px',
      }}
    >
      1
    </Stack>
  );
};
